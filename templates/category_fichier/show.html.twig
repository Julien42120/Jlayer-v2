{% extends 'base.html.twig' %}

{% block title %}CategoryFichier{% endblock %}

{% block body %}
    <h1>CategoryFichier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_fichier.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ category_fichier.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_fichier_index') }}">back to list</a>

    <a href="{{ path('category_fichier_edit', {'id': category_fichier.id}) }}">edit</a>

    {{ include('category_fichier/_delete_form.html.twig') }}
{% endblock %}
